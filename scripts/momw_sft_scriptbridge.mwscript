Begin momw_sft_scriptbridge

short cost
short deductToken

if cost > 0
    player->removeitem gold_001 cost
    set cost to 0
endif

if deductToken > 0
    player->removeitem momw_sft_travel_token 1
    set deductToken to 0
endif

End momw_sft_scriptbridge
